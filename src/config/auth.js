export const authConfig = {
  token: '@App-Token',
  userData: '@App-User', // only store if endpoint to get user from backend isn't available
  loginEndpoint: ''
  // other auth endpoints should come in here
};
